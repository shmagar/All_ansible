- hosts: webservers
  #connection: local
  become: true
  remote_user: shraddha_magar5
  gather_facts: true
  roles:
  - appserver

# hosts: webservers
 # become: true
#  remote_user: shraddha_magar5
 # gather_facts: false
  #roles:
 # - appserver

